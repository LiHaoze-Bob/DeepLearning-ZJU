#import "template.typ": project
#import "images/final/network-overview.typ": network-overview

#show: project.with(
  theme: "lab",
  course: "课程综合实践I-\n深度学习基础——理论与实践",
  title: "课程设计实验报告",
  name: "基于迁移学习的 CIFAR-10 图像分类",
  author: "李昊泽",
  school_id: "3250102780",
  college: "计算机科学与技术学院",
  major: "计算机科学与技术",
  teacher: "王总辉",
  place: "紫金港海洋大楼11机房",
  date: "2026年9月12日",
  font_serif: ("Songti SC", "New Computer Modern"),
  font_sans_serif: ("PingFang SC", "Helvetica Neue"),
  font_mono: ("Monaco",),
)

// 本报告只引用已有实验结果，不触发训练，也不依赖占位截图。
// 图 1 为按实现绘制的网络结构图；图 2、3 为 Notebook 内嵌原图。
// 图 4 为真实代码和输出节选的导出截图。
#set text(size: 11pt)
#set par(leading: 0.5em, spacing: 0.65em, first-line-indent: 2em)
#set heading(numbering: none)
#show heading.where(level: 1): it => block(above: 1em, below: 0.75em)[
  #text(size: 14pt, weight: "bold")[#it.body]
]
#show heading.where(level: 2): it => block(above: 0.9em, below: 0.5em)[
  #text(size: 11.5pt, weight: "bold")[#it.body]
]
#set table(inset: (x: 5pt, y: 5pt), stroke: 0.4pt + luma(155))
#show figure.caption: set text(size: 9.5pt)
#set figure(supplement: [图])
#let small-table(body) = context {
  set text(size: 10pt)
  body
}

= 一、实验目标与网络概览

本次课程设计使用课程提供的 CIFAR-10 数据完成十分类，并以飞机（0）、猫（3）、青蛙（6）三类准确率的平均值作为主要评价指标。我先在本地训练 Wide ResNet 验证数据与推理流程，再采用 ImageNet-1K 预训练的 ConvNeXt-Tiny，在 Kaggle 单张 Tesla T4 上微调。最终模型在 10,000 张验证图像上取得 *98.67%* 的总体准确率，三类平均准确率为 *98.63%*。



#figure(
  network-overview(),
  kind: image,
  caption: [本项目网络结构与训练、推理路径。主干图展示单次前向；TTA 复用同一模型，非两个独立模型。],
)

#pagebreak()

= 二、实验原理


== 1. ConvNeXt 残差块与特征提取

#link("https://docs.pytorch.org/vision/0.25/models/generated/torchvision.models.convnext_tiny.html")[ConvNeXt-Tiny] 的块内计算可概括为“7×7 逐通道卷积 → LayerNorm → 通道扩展到 4 倍 → GELU → 通道投影”，之后经过 Layer Scale 和 stochastic depth，与输入作残差相加。忽略训练中的随机丢弃时，可以简写为 $y = x + gamma F(x)$，其中 $gamma$ 是可学习的逐通道缩放系数。

逐通道卷积先在每个通道内提取空间模式，再由通道投影混合不同通道的信息。残差连接提供直接的信息和梯度通路，使网络可以学习对输入特征的修正。LayerNorm 在每个空间位置沿通道归一化，不依赖 batch 内不同样本的统计量；stochastic depth 则在训练时随机跳过部分残差分支，并在推理时恢复全部分支。这些机制分别服务于特征建模、优化稳定性和正则化。

== 2. 数据增强、标签平滑与 CutMix

随机裁剪、水平翻转和 RandAugment 在标签语义基本不变的前提下改变输入，促使模型不要过度依赖固定位置、朝向或颜色。增强强度也不能无限增加：CIFAR-10 原图只有 32×32，过强的几何和颜色变化可能破坏本来就有限的类别线索。

对于十分类 one-hot 标签 $y$，标签平滑将目标改为 $q_k = (1-epsilon)y_k + epsilon / 10$。本实验取 $epsilon=0.05$，正确类目标为 0.955，其余每类为 0.005；损失为软标签交叉熵 $L = -sum_(k=1)^10 q_k ln p_k$。它减弱了模型将训练样本置信度推向极端的倾向，但不意味着预测概率已经得到良好校准。

#link("https://arxiv.org/abs/1905.04899")[CutMix] 用另一张图像的矩形区域替换当前图像的一部分。设二值掩码 $M$ 在保留原图的位置为 1，则：

$ tilde(x) = M dot x_a + (1-M) dot x_b, quad tilde(q) = lambda q_a + (1-lambda)q_b. $

其中乘法按像素逐元素进行，$lambda$ 是原图实际保留的面积比例。矩形被图像边缘裁切后要重新计算该比例，不能直接使用最初采样值。模型因此需要从部分可见区域判断类别；训练目标变得更软、更难，也解释了启用 CutMix 后 loss 与验证准确率未必同步变化。



== 3. AdamW、warmup 与余弦退火

Adam 根据梯度的一阶、二阶矩估计调整各参数的更新尺度。#link("https://arxiv.org/abs/1711.05101")[AdamW] 在此基础上把权重衰减与自适应梯度更新分开，其更新可概括为：

$ theta_(t+1) = (1-eta_t lambda_w)theta_t - eta_t hat(m)_t / (sqrt(hat(v)_t)+epsilon). $

这里 $hat(m)_t$、$hat(v)_t$ 是偏差修正后的矩估计，$lambda_w$ 为衰减系数。与把 L2 项直接加入 Adam 的梯度不同，解耦的衰减不会再经过二阶矩的逐参数缩放。本实验对卷积、线性层权重使用 0.05 的衰减，对 bias 和一维归一化参数不衰减。

warmup 在微调初期逐步增大学习率，缓和解冻骨干后的更新；余弦退火则在后期逐渐缩小步长。对本实现，warmup 后的倍率为 $r(p)=0.02+0.98(1+cos(pi p))/2$，$p$ 表示该段调度的归一化进度，实际学习率为基础学习率乘以 $r(p)$。当前保留 30 轮调度但只执行了 9 轮微调，因此没有到达余弦曲线的末端。

== 4. EMA 与测试时增强：平滑对象不同

EMA 平滑的是训练过程中的模型参数。若第 $t$ 次更新的参数为 $theta_t$，则维护：

$ overline(theta)_t = d_t overline(theta)_(t-1) + (1-d_t)theta_t. $

本实验采用 $d_t=min(0.999,(1+t)/(10+t))$，初期较小的衰减让 EMA 较快跟随刚开始适应任务的分类头，后期再提高平滑程度。这样可以减弱单次 mini-batch 更新的波动，但 EMA 并不保证一定优于原始权重，仍需要验证结果判断。

TTA 平滑的是同一输入不同视图下的预测。评估时对原图和水平翻转图各进行一次前向，将 logits 平均：

$ z(x) = (f_(overline(theta))(x)+f_(overline(theta))(T(x))) / 2. $

其中 $T$ 为水平翻转。它在两个视图都保持类别语义时可能减少方向敏感性，代价是两次前向计算。实现中平均的是 logits，不是先做 softmax 后平均概率；这两种操作通常不等价。EMA 与 TTA 可以同时使用，但分别属于参数层面和预测层面的平滑。

== 5. AMP、梯度累积与梯度裁剪

#link("https://docs.pytorch.org/docs/2.10/amp.html")[自动混合精度（AMP）] 为适合的运算选择低精度，并保留必要的高精度计算；GradScaler 通过先放大 loss、反向传播后再还原梯度，降低 FP16 小梯度下溢的风险。检测到溢出时可跳过本次优化器更新，EMA 也应跳过同步。

梯度累积将两个 batch 的梯度按样本数加权后再更新一次，32 的实际 batch 因而对应 64 的等效 batch；这减少单次前向需要保留的激活，但不是免费提速。梯度裁剪则限制梯度整体范数，抑制异常大的更新。实现中先撤销梯度缩放，再按最大范数 1.0 裁剪，避免把放大后的梯度误当作真实梯度处理。

#pagebreak()

= 三、模型实现

== 1. 数据划分与评价指标

#small-table(table(
  columns: (1fr, 2fr, 1fr, 1fr),
  table.header([*用途*], [*课程文件*], [*总数*], [*每类数量*]),
  [训练], [data\_batch\_1 至 data\_batch\_5], [50,000], [5,000],
  [验证、选择模型], [test\_batch], [10,000], [1,000],
))

数据读取后检查形状为 $(N,32,32,3)$、标签范围为 0 至 9，并逐类统计数量。只有训练集参与梯度更新；验证集只用于评估与选择权重。微调阶段没有另外加入外部图像，但模型初始化使用了外部 ImageNet-1K 预训练知识。

模型仍输出全部十类。设第 $c$ 类的正确数为 $n_c$、样本数为 $N_c$，则 $A_c=n_c/N_c$，评分指标 $S=(A_0+A_3+A_6)/3$。每轮记录总体准确率、十类准确率和混淆矩阵；选模时优先比较 $S$，仅在 $S$ 相同时比较总体准确率。

== 2. 从本地基线到迁移学习

最初采用 WRN-16-2 从零训练，模型较小，便于在本地 CPU 上检查十分类训练、模型保存及重载流程。归档 Notebook 的 20 轮训练记录对应的最佳模型总体准确率为 87.15%，其中猫类只有 71.70%，限制了评分三类的平均值。随后我将重点转向预训练特征的适配，希望在有限训练时间内得到更好的分类能力。

#small-table(table(
  columns: (2.1fr, 1fr, 1fr, 1fr, 1fr),
  table.header([*已完成方案*], [*总体*], [*飞机*], [*猫*], [*青蛙*]),
  [WRN-16-2，本地从零训练], [87.15%], [87.80%], [71.70%], [93.50%],
  [ConvNeXt-Tiny，Kaggle 微调], [98.67%], [99.60%], [96.70%], [99.60%],
))

两者的三类平均准确率分别为 84.33% 和 98.63%。不过，这并不是只改变“是否预训练”的对照实验：架构、分辨率、优化器和训练设备也同时变化。

== 3. ConvNeXt-Tiny 的十分类改造

本实验直接使用 TorchVision 的官方实现及 `IMAGENET1K_V1` 权重，将末层从 1,000 类改为 10 类，重新初始化分类层。修改后的参数量为 27,827,818。四个特征阶段的通道数与块数见图 1，训练时最大 stochastic depth 概率设为 0.1。训练新分类头时冻结特征提取部分，再解冻全部网络。

== 4. 输入处理与分辨率

训练增强先在原始图像上完成：反射填充 4 像素的随机裁剪、水平翻转，以及 RandAugment（2 个操作，强度 7）。模型内部再双线性插值到 224×224，并使用 ImageNet 的均值 $(0.485,0.456,0.406)$ 和标准差 $(0.229,0.224,0.225)$ 归一化。这里没有照搬 ImageNet 默认的 236 缩放加中心裁剪，而是保留整幅 CIFAR-10 图像。

放大不会凭空恢复低分辨率中丢失的纹理。我采用 224 输入，主要是为了让预训练网络在更接近原有空间尺度的条件下工作，代价是计算量增加。是否能缩小到 160 或 192 并保持成绩，需要额外对照实验，本次没有作出结论。

#pagebreak()

= 四、训练实现

== 1. 两阶段微调与主要参数

第一阶段只训练新分类头及分类部分的归一化参数，让随机初始化的输出层先适应十分类。第二阶段解冻全网络，骨干学习率设置为分类头的十分之一，减少对已有特征的剧烈更新。两阶段均使用 AdamW。

#small-table(table(
  columns: (1.3fr, 2.7fr),
  table.header([*项目*], [*本次正式运行配置*]),
  [分类头阶段], [2 epochs；学习率 0.001；骨干冻结],
  [全网络阶段], [最多 10 epochs，实际 9；骨干 0.00005，分类头 0.0005],
  [学习率调度], [微调前 3 轮 warmup；沿用 30 轮配方的余弦调度],
  [Batch 与累积], [训练 batch 32，梯度累积 2，等效 batch 64；验证 batch 32],
  [Weight decay], [0.05；bias 和一维归一化参数不衰减],
  [损失与标签], [软标签交叉熵；label smoothing = 0.05],
  [CutMix], [微调时以 0.25 概率执行；Beta(1,1) 采样混合比例],
  [数值与显存], [CUDA AMP、channels\_last、梯度范数裁剪 1.0],
  [EMA], [最大衰减 0.999；初期渐增衰减],
  [验证], [EMA 权重，原图与水平翻转两视图 TTA],

))
\
\
实现时先平滑标签，再按实际替换面积执行 CutMix 标签混合。分类头阶段关闭 CutMix，全网络微调阶段按 25% 概率启用；验证时不进行训练增强。
\
\
EMA 对训练权重作指数平滑，每次有效的优化器更新后才同步；AMP 因溢出跳过更新时，EMA 也不更新。评估时用同一个模型分别预测原图和水平翻转图，再平均 logits。最终保存的是一份 EMA 模型权重，没有使用多个独立模型的集成。

== 2. 配方轮数与实际执行轮数

配置中保留的 30 轮是学习率调度的参考长度，不是本次实际跑了 30 轮。执行上限由 10 轮微调限制，预算不足又使第 10 轮未开始。相应地，本次并没有走完余弦退火，也没有进入原配方“最后 5 轮关闭 CutMix”的阶段；实际 9 轮微调均允许以 25% 概率执行 CutMix。




#pagebreak()

= 五、训练过程与总体结果

#figure(
  image("images/final/learning-curves.png", width: 100%),
  caption: [Notebook 原始学习曲线。横轴为累计 epoch，前 2 轮训练分类头，第 3 轮开始全网络微调；准确率来自 EMA + 双视图 TTA。],
)

#small-table(table(
  columns: (1.5fr, 1fr, 1fr, 1fr),
  table.header([*阶段 / 轮次*], [*训练 loss*], [*总体准确率*], [*目标三类均值*]),
  [分类头 1], [0.6830], [93.66%], [92.73%],
  [分类头 2], [0.5922], [93.95%], [92.97%],
  [微调 1], [0.6793], [96.93%], [95.87%],
  [微调 2], [0.5942], [97.72%], [97.37%],
  [微调 3], [0.5539], [98.10%], [97.73%],
  [微调 4], [0.5193], [98.28%], [97.90%],
  [微调 5], [0.5154], [98.49%], [98.13%],
  [微调 6], [0.4883], [98.51%], [98.20%],
  [微调 7], [0.5013], [98.55%], [98.20%],
  [微调 8], [0.4921], [98.50%], [98.30%],
  [*微调 9*], [*0.4726*], [*98.67%*], [*98.63%*],
))

冻结骨干训练 2 轮后，三类均值为 92.97%；解冻后的第一轮即提高到 95.87%，说明当前预训练特征经过任务适配后还有明显提升空间。进入微调时 loss 从 0.5922 上升到 0.6793，但验证准确率反而提高。
\
\
模型选择规则也在日志中得到了具体体现：微调第 7 到第 8 轮，总体准确率从 98.55% 降到 98.50%，三类均值却从 98.20% 升到 98.30%，因此仍更新最佳权重。第 9 轮再次同时改善两个指标，成为最终选择的第 11 个累计 epoch。



#pagebreak()

= 六、结果与误差分析

最终使用最佳 EMA 权重重新评估全部 10,000 张验证图像。正确分类 9,867 张；评分三类共正确分类 2,959 / 3,000 张，均值为 $(99.60% + 96.70% + 99.60%) / 3 = 98.63%$（四舍五入）。下表中加粗的类别是课程评分类别，各类均有 1,000 张图像。

#small-table(table(
  columns: (1.4fr, 1fr, 1fr, 1.4fr, 1fr, 1fr),
  table.header([*类别*], [*正确数*], [*准确率*], [*类别*], [*正确数*], [*准确率*]),
  [*飞机*], [996], [*99.60%*], [狗], [971], [97.10%],
  [汽车], [990], [99.00%], [*青蛙*], [996], [*99.60%*],
  [鸟], [989], [98.90%], [马], [994], [99.40%],
  [*猫*], [967], [*96.70%*], [船], [994], [99.40%],
  [鹿], [985], [98.50%], [卡车], [985], [98.50%],
))

#figure(
  image("images/final/confusion-matrix.png", width: 86%),
  caption: [Notebook 原始混淆矩阵。行为真实类别，列为预测类别；对角线是正确分类数量。],
)

目标三类共错分 41 张，其中猫类占 33 张，约为 80.5%。飞机和青蛙已经各只错 4 张，后续若要提高三类平均分，猫类是更值得排查的瓶颈。但这不意味着可以忽略狗等非评分类别：猫被误判为狗 23 次，狗被误判为猫也为 23 次，两种方向合计占全部 133 次错误的约 34.6%。学好相近类别之间的边界，对目标类别同样重要。

低分辨率、姿态与背景可能使猫狗的区分更加困难，不过混淆矩阵只能证明错误集中在哪里，不能单独证明原因。下一步应查看这 46 张错分图像，再判断问题主要来自主体细节、遮挡还是背景干扰，而不是看到数字后直接认定模型“只学到了背景”。

#pagebreak()

= 七、运行检查



#figure(
  image("images/final/verification-output.png", width: 100%),
  caption: [Notebook 第 21、24 节已保存的代码与输出节选，由导出内容渲染截图。重载检查使用 2 张真实验证图像，不代表新一次训练。],
)

最终用 `torch.save` 保存 `net.state_dict()`，导出文件名为 cifar10\_convnext\_tiny.pth。重建时关闭预训练下载，用 `map_location` 加载完整状态，再进入评估模式。图中检查了保存前后 logits 在给定浮点容差内接近、预测标签一致、输出形状正确、数值有限，以及 `net.training` 为假。

#pagebreak()

= 八、讨论与改进

以下改进围绕当前模型的误差和计算成本展开，尚未实际执行；比较时应保留同一基线，避免同时改变多个因素后无法解释结果。

== 1. 优先排查猫狗边界，而不是只增加目标类别权重

目标三类的 41 次错误中有 33 次来自猫，且猫狗互判共 46 次。后续应先整理这些错分图像的预测置信度，并按主体过小、遮挡、背景复杂等现象分组，再检查训练集中是否缺少相应模式。若错误集中于主体位置，可对比更温和的裁剪或遮挡增强；如果主体细节已经被原图分辨率限制，单纯提高插值尺寸未必有效。

这类改动仍应训练完整十类，记录猫类召回、狗类召回及两个方向的混淆数量。仅提高猫类权重可能把更多狗误判成猫，因此不能只看猫类一个指标。公开验证集的错分图像用于诊断，不应直接并入训练集。

== 2. 让学习率与增强策略匹配短训练周期

当前微调只完成 9 轮，却沿用 30 轮的学习率和 CutMix 关闭时间。可先只将调度长度改为计划执行的 10 轮，保留 3 轮 warmup，比较同一时间预算下的最优三类均值；再单独测试最后 2 轮关闭 CutMix，让模型在收尾阶段适应未混合样本。两项修改应分开实验，不能把可能的改善预先写成既定收益。

固定轮数不一定对应固定成本，因此比较时还应记录达到 95%、98% 三类均值所需的时间，以及最佳权重出现的轮次。若短调度反而更早停滞，可考虑延长高学习率阶段，而不是默认越快降到低学习率越好。

== 3. 通过消融确定分辨率、EMA 与 TTA 是否值得保留

先对同一份最佳权重比较单视图与双视图 TTA，记录三类均值、十类总体准确率和推理耗时；这项检查不需要重新训练。之后分别在 160、192、224 输入尺寸下按相同预算训练，避免只在 224 训练的权重上临时改尺寸，就宣称得到了低分辨率训练结论。EMA 的作用则应在同一训练轨迹中比较原始权重和 EMA 权重。

若某项方法带来的精度变化小于重复实验的波动，却明显增加耗时，就应优先选择更简单的配置。标签平滑和 CutMix 强度也可逐项消融，但不宜在一次实验中同时修改模型、分辨率和增强策略。



= 参考资料

+ 课程资料
+ 本报告的部分文字组织、代码核对与排错分析由 OpenAI Codex 辅助完成。所有实验数据、运行输出和技术描述均由本人检查，最终报告由本人审阅并负责。
