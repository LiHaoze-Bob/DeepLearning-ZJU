#import "template.typ": project, note, tip, warning, important, analysis

#show: project.with(
  theme: "lab",
  course: "课程综合实践I-
深度学习基础——理论与实践",
  title: "实验四实验报告",
  name: "实验四：CNN工程化与数据增强",
  author: "李昊泽",
  school_id: "3250102780",
  college: "计算机科学与技术学院",
  major: "计算机科学与技术",
  teacher: "王总辉",
  place: "紫金港海洋大楼11机房",
  date: "2026年9月3日",
  font_serif: ("Songti SC", "New Computer Modern"),
  font_sans_serif: ("PingFang SC", "Helvetica Neue"),
  font_mono: ("Monaco",),
)

#let screenshot(title, suggestion, height: 6cm) = figure(
  block(
    width: 100%,
    height: height,
    fill: white,
    stroke: (paint: luma(165), thickness: 0.8pt, dash: "dashed"),
    radius: 4pt,
    inset: 14pt,
    align(center + horizon)[
      #text(size: 12pt, weight: "bold", fill: luma(85))[截图留空区]
      #v(0.8em)
      #text(size: 10pt, fill: luma(115))[#suggestion]
    ],
  ),
  kind: image,
  caption: title,
)

= 摘要

第一部分在 Kaggle 平台完成 48×48 灰度人脸表情七分类，使用在线随机增强、三阶段 CNN、带类别权重和标签平滑的交叉熵、AdamW、余弦退火、混合精度、早停及测试时增强。第二部分在 LMCC 实验中完成 28×28 美国手语图像的 24 分类，通过随机裁剪、水平翻转、旋转以及亮度和对比度扰动改善模型泛化，并将训练模型保存后用于新图片推理。
\
\





= 实验原理

== 数据增强与泛化

当训练准确率显著高于验证准确率时，模型可能记住训练样本的局部细节，而没有学到对新图片稳定的特征。数据增强通过随机变换构造新的训练视图。若原始图像为 $x$，随机变换参数为 $theta$，增强后的输入可以写为：

$ x' = T(x; theta) $

增强强度必须符合任务语义。人脸和手势可能左右翻转，但通常不会上下倒置；过大的旋转还可能改变手语字母本身。因此本实验不采用垂直翻转，并限制旋转和缩放范围。


== 激活与正则化

Kaggle 表情模型采用 RReLU。对于负输入，它保留随机小斜率：

$ f(x) = cases(x & x >= 0, a x & x < 0) $

这可以降低普通 ReLU 在负半轴梯度恒为 0 所造成的神经元失活风险。ASL 模型使用 ReLU，即 $f(x)=max(0,x)$。BatchNorm 用于稳定不同层的激活尺度；Dropout 在训练阶段随机屏蔽部分激活，减少网络对少数神经元的依赖。
\
\
`CrossEntropyLoss` 已在内部完成 LogSoftmax 和负对数似然计算，预测时直接对 logits 使用 `argmax` 即可得到类别。



= 实验一 Kaggle 表情图片分类

== 任务

任务将人脸表情划分为七类：0 生气、1 厌恶、2 恐惧、3 高兴、4 难过、5 惊讶、6 中立。训练集包含 21998 张图片，测试集包含 6711 张图片。每张图像由 `feature` 字段中的 2304 个灰度值表示，对应 48×48 像素；训练标签位于 `label` 列，测试样本编号位于 `id` 列。

#table(
  columns: (0.8fr, 1fr, 0.8fr, 1fr, 0.8fr, 1fr, 0.8fr, 1fr),
  inset: 5pt,
  [*类别*], [*数量*], [*类别*], [*数量*], [*类别*], [*数量*], [*类别*], [*数量*],
  [0], [3062], [1], [355], [2], [3188], [3], [5529],
  [4], [3692], [5], [2375], [6], [3797], [], [],
)

#note[类别 1 只有 355 个样本，而类别 3 有 5529 个样本，类别分布明显不均衡。训练时采用经过限制的类别权重，减少模型只偏向多数类的现象。]


== 数据增强

数据预处理与实验三基本相同，这里主要介绍如何进行数据增强。
\
\
在线数据增强只配置给 `train_set`，在 `Dataset.__getitem__()` 每次取样时执行；验证集和测试集只做标准化。增强参数如下：

#table(
  columns: (1.55fr, 1.55fr, 2.5fr),
  inset: 6pt,
  [*增强方法*], [*参数*], [*作用*],
  [随机水平翻转], [`p=0.5`], [适应左右方向变化],
  [随机仿射], [`±12°，平移 8%`], [模拟姿态和位置的小幅变化],
  [随机缩放], [`0.92～1.08`], [适应人脸大小差异],
  [亮度与对比度], [`0.15 / 0.15`], [减弱光照差异影响],
  [Random Erasing], [`p=0.20`], [模拟局部遮挡并增强鲁棒性],
)

#figure(
  image(
    "../assets/17.png",
  ),
  caption: [数据增强效果图],
)

== 网络结构

模型保持前一次实验中的基本架构

#table(
  columns: (1.25fr, 2.1fr, 2.4fr),
  inset: 6pt,
  [*阶段*], [*输入与输出*], [*主要配置*],
  [卷积阶段 1], [`1×48×48 → 64×24×24`], [两次 3×3 Conv，Pool，Dropout 0.05],
  [卷积阶段 2], [`64×24×24 → 128×12×12`], [两次 3×3 Conv，Pool，Dropout 0.10],
  [卷积阶段 3], [`128×12×12 → 256×6×6`], [两次 3×3 Conv，Pool，Dropout 0.15],
  [Flatten], [`256×6×6 → 9216`], [展开二维特征图],
  [分类器], [`9216 → 1024 → 256 → 7`], [BN、RReLU 与多级 Dropout],
)

#figure(
  image(
    "../assets/18.png",
  ),
  caption: [模型打印结果及可训练参数数量],
)

== 训练与预测策略

模型使用带类别权重和 0.05 标签平滑的交叉熵损失。优化器为 AdamW，初始学习率为 0.001，权重衰减为 $2 times 10^(-4)$；学习率通过余弦退火逐步降低到 $10^(-5)$。最多训练 35 个 epoch，并使用自动混合精度、梯度裁剪和早停。验证准确率提高时保存 `best_lab4_expression_cnn.pth`，连续 8 轮未提升则停止训练。
\
\


== 实验结果与分析

该部分指标应以 Kaggle Notebook 完整运行后的输出为准。完成运行后填写下表，并将对应界面截图插入预留区域。

#table(
  columns: (1.7fr, 1.2fr, 1.8fr, 1.2fr),
  inset: 7pt,
  [*指标*], [*结果*], [*指标*], [*结果*],
  [最佳验证准确率], [0.6312], [最佳验证 Loss], [1.2284],
  [实际训练轮数], [35], [Kaggle Accuracy], [0.63418],
)

#figure(
  image(
    "../assets/19.png",
  ),
  caption:[lab4训练曲线],
)
\
\
比较这张训练曲线和lab3的训练曲线（在lab3的实验报告中），我们可以发现：经过数据增强后训练曲线更加平滑，Loss降低，Accuracy提高，并且训练和验证的曲线相交更少。

\
\
#figure(
  image(
    "../assets/20.png",
  ),
  caption:[混淆矩阵], 
)
对比前一次实验可以发现，数据增强后六个类别的召回率均有所提高，七类平均召回率约由 56.6% 提升至 60.3%。不过，Disgust 类别的召回率由 64% 降至 53%，说明增强策略对不同类别的作用并不完全一致，后续可结合类别样本数量调整增强强度或采用更有针对性的增强方法。





#pagebreak()

= 实验二 美国手语图片识别与部署

== 任务

本部分对 28×28 单通道美国手语图片进行分类。静态数据不包含需要连续运动表示的 J 和 Z，因此模型输出 24 个类别。训练 DataLoader 使用批大小 32 并打乱样本，验证 DataLoader 保持固定顺序。

== 网络结构


#table(
  columns: (1.2fr, 2fr, 2.5fr),
  inset: 6pt,
  [*阶段*], [*输入与输出*], [*层配置*],
  [卷积块 1], [`1×28×28 → 25×14×14`], [Conv、BN、ReLU、Pool],
  [卷积块 2], [`25×14×14 → 50×7×7`], [Conv、BN、ReLU、Dropout 0.2、Pool],
  [卷积块 3], [`50×7×7 → 75×3×3`], [Conv、BN、ReLU、Pool],
  [Flatten], [`75×3×3 → 675`], [展开卷积特征],
  [全连接], [`675 → 512 → 24`], [Dropout 0.3、ReLU],
)

模型使用普通 `CrossEntropyLoss` 和 Adam 优化器。

== 数据增强

ASL 训练将多种增强操作组合为 `random_transforms`：

```python
random_transforms = transforms.Compose([
    transforms.RandomRotation(5),
    transforms.RandomResizedCrop(
        (28, 28), scale=(.9, 1), ratio=(1, 1)
    ),
    transforms.RandomHorizontalFlip(),
    transforms.ColorJitter(brightness=.2, contrast=.5)
])
```

增强在训练函数的 `model(random_transforms(x))` 中执行，而验证函数直接使用 `model(x)`。这样可以确保训练阶段每轮接触不同变体，同时使验证指标保持可比较。旋转限制为 5°，避免较大角度改变手势字母的含义；水平翻转可以模拟左右手差异，但不使用不符合真实场景的垂直翻转。


#figure(
  image(
    "../assets/21.png",
  ),
  caption: [数据增强效果],
)

== 训练结果

模型训练 20 个 epoch，代表性结果如下：

#table(
  columns: (0.7fr, 1.15fr, 1fr, 1.15fr, 1fr),
  inset: 6pt,
  [*Epoch*], [*训练 Loss*], [*训练 Acc*], [*验证 Loss*], [*验证 Acc*],
  [0], [647.6953], [75.51%], [58.9038], [90.32%],
  [3], [38.2481], [98.54%], [21.8050], [97.27%],
  [6], [29.8795], [98.90%], [13.0882], [98.33%],
  [10], [21.2882], [99.18%], [11.4074], [99.00%],
  [12], [15.5849], [99.45%], [110.0979], [90.52%],
  [15], [9.6484], [99.61%], [14.1347], [98.47%],
  [19], [10.6165], [99.61%], [8.8347], [98.65%],
)
\
\

验证准确率由 Epoch 0 的 90.32% 快速上升，在 Epoch 10 达到最高值 99.00%，最后一轮为 98.65%。Epoch 12 的验证准确率曾下降到 90.52%，但后续恢复，表明随机增强和参数更新仍会带来一定波动。训练准确率后期约为 99.6%，与最佳验证准确率差距较小，相比训练集接近满分而验证集明显落后的情况，过拟合得到缓解。

== 模型部署与预测

部署阶段重新加载前一部分保存的模型：

```python
model = torch.load(
    "model.pth", map_location=device, weights_only=False
)
```
\
\
新图片先读取为灰度图，再转换为浮点型并缩放到 0～1，调整为 28×28，增加 batch 维度后送入模型。输出是 24 个 logits，并不是概率；取最大 logit 的索引，再通过字符串 `abcdefghiklmnopqrstuvwxy` 映射到字母。
\
\


#figure(
  image(
    "../assets/22.png",
  ),
  caption:[B预测失败],
)

#figure(
  image(
    "../assets/23.png",
  ),
  caption: [A预测成功],
)



`B.jpg` 被识别为 e，则说明高分辨率拍摄图片缩放到 28×28 后，手指细节可能丢失，而且背景、光照、手部大小和姿态与训练集存在分布差异。改进方向包括增加真实拍摄样本、统一裁剪手部区域、使用更接近部署场景的增强。


= 实验总结

本实验通过两个任务验证了数据增强在 CNN 工程流程中的作用。在线增强不需要永久复制数据，而是在训练时不断生成随机变体，从而提高样本多样性；验证集不增强，保证指标可以反映模型在固定数据上的泛化能力。
\
\
后续可以根据 Kaggle 完整运行结果调整增强强度、类别权重和网络容量，并根据混淆矩阵重点改善相近表情类别；对于 ASL 部署，则应增加真实场景数据并采用最佳验证权重，使训练分布与实际输入更加一致。


\
\
\
\
\
\
= 参考资料

+ 课程资料
+ 本报告的部分文字组织、代码核对与排错分析由 OpenAI Codex 辅助完成。所有实验数据、运行输出和技术描述均由本人检查，最终报告由本人审阅并负责。
