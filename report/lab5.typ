#import "template.typ": project

#show: project.with(
  theme: "lab",
  course: "课程综合实践I-\n深度学习基础——理论与实践",
  title: "实验五实验报告",
  name: "实验五：迁移学习",
  author: "李昊泽",
  school_id: "3250102780",
  college: "计算机科学与技术学院",
  major: "计算机科学与技术",
  teacher: "王总辉",
  place: "紫金港海洋大楼11机房", // 按实际实验地点填写。
  date: "2026年9月4日", // 按实际实验日期填写。
  font_serif: ("Songti SC", "New Computer Modern"),
  font_sans_serif: ("PingFang SC", "Helvetica Neue"),
  font_mono: ("Monaco",),
)

// 补图方法：将对应 screenshot(...) 整段替换为
// #figure(image("images/lab5-01.png", width: 100%), caption: [对应图题])
// height 可调整留空高度；本报告不依赖尚未提供的图片文件。
#let screenshot(title, suggestion, height: 4.8cm) = figure(
  block(
    width: 100%, height: height,
    stroke: 0.6pt + luma(170),
    inset: 10pt, breakable: false,
    align(top + left)[
      #text(size: 9pt, fill: luma(115))[截图留空：#suggestion]
    ],
  ),
  kind: image,
  caption: title,
)

#let pending = text(fill: luma(100))[待填写]
#set table(inset: 6pt)

= 摘要

车辆分类部分构建普通 CNN 作为对照，并采用 ImageNet 预训练 VGG16-BN，先训练新分类头，再解冻最后一个卷积块进行小学习率微调。1646 条数据按 D5 的原顺序划分为 1481 条训练样本和 165 条验证样本。CNN 最佳验证准确率为 `86.06%`，VGG16-BN 最终保留模型为 `94.55%`，提高约 8.48 个百分点。
\
\
智能狗门部分先直接使用预训练 VGG16 区分狗、猫和其他对象，再添加单输出线性层，训练只允许柯基犬 Penny 进入的个性化狗门。Penny 模型冻结阶段最高验证准确率为`90.91%`，冻结阶段末轮及微调结束时均为 `81.82%`。


= 实验目的

+ 理解迁移学习如何复用预训练模型的特征，并掌握冻结参数、替换或添加分类层、小学习率微调的方法。
+ 在相同数据划分下比较自定义 CNN 和迁移学习模型的验证准确率。
+ 理解类别标签、logits、损失函数和判定阈值的关系，并完成车辆分类提交及个性化狗门判断。

#pagebreak()
= 实验原理

== 预训练特征与迁移学习

迁移学习把在较大数据集上训练得到的参数作为新任务的起点，使小数据集不必从随机参数开始学习全部特征。
\
\

将模型写为 $z = g_phi(f_theta(x))$，其中 $f_theta$ 表示复用的网络部分，$g_phi$ 表示新分类层。冻结阶段固定 $theta$，只更新 $phi$；微调阶段允许部分或全部 $theta$ 参与更新，并使用较小学习率：

$ theta_(t+1) = theta_t - eta "∇"_theta L $

先训练分类头，是为了让随机初始化的新层先适应已有表示；随后再调整预训练参数，减少训练初期较大更新对原有特征的破坏。微调是否有效仍需由验证结果判断。

== 两种分类头的区别

#table(
  columns: (1.05fr, 1.75fr, 1.2fr),
  [任务], [实际网络改动], [输出与损失],
  [车辆分类], [替换 VGG16-BN 最后的线性层：4096 → 类别数 $C$], [$C$ 个 logits；交叉熵],
  [Penny 狗门], [保留完整 VGG16，在 1000 维输出后添加 Linear(1000, 1)], [1 个 logit；二元交叉熵],
)

车辆分类模型直接接收未归一化的类别分数，由 `CrossEntropyLoss` 计算损失。若真实类别为 $y$，则单样本损失为：

$ L = -log(exp(z_y) / sum_(k=0)^(C-1) exp(z_k)) $

Penny 模型使用 `BCEWithLogitsLoss`，内部结合 Sigmoid 和二元交叉熵。设 $p = sigma(z) = 1 / (1 + exp(-z))$，则：

$ L = -y log(p) - (1-y) log(1-p) $



== 冻结参数

`requires_grad_(False)` 控制参数是否计算梯度；`train()` 和 `eval()` 控制 Dropout、BatchNorm 等层的行为；`torch.no_grad()` 关闭梯度记录。三者作用不同。即使基础网络参数已被冻结，调用整个模型的 `train()` 仍会开启其中的 Dropout。



#pagebreak()
= 实验一：Kaggle 车辆图像分类

== 数据处理

训练图像转换为 RGB，随机裁剪为 224×224，进行水平翻转与轻量颜色扰动，再转为张量并使用 ImageNet 均值、标准差归一化。验证与提交图像采用短边缩放到 256、中心裁剪到 224×224 和相同归一化。输入张量形状为 $B times 3 times 224 times 224$。


== 普通 CNN 对照模型

对照模型使用两组卷积、批归一化、ReLU 和最大池化提取特征，再通过自适应平均池化和全连接层完成分类。其主要尺寸变化如下：

#table(
  columns: (1.75fr, 1fr),
  [网络层], [输出尺寸],
  [输入], [3×224×224],
  [3×3 卷积，3 → 16；BN、ReLU、2×2 池化], [16×112×112],
  [3×3 卷积，16 → 32；BN、ReLU、2×2 池化], [32×56×56],
  [自适应平均池化], [32×7×7],
  [Flatten、Dropout(0.35)、Linear、ReLU], [1568 → 128],
  [Dropout(0.20)、Linear], [128 → $C$],
)

当前二分类模型共有 206,226 个可训练参数，输出形状为批大小×2。模型采用 Adam，学习率为 $10^(-3)$，交叉熵损失，训练 15 轮。实际运行的 batch size 为 32。

== VGG16-BN 冻结训练与微调

加载 ImageNet 预训练 VGG16-BN 后，首先冻结原有参数，再将最后一层替换为适合本任务的线性层。由于新层是在冻结之后创建，其参数仍可训练。

#block(breakable: false)[
```python
for parameter in transfer_model.parameters():
    parameter.requires_grad = False

in_features = transfer_model.classifier[-1].in_features
transfer_model.classifier[-1] = nn.Linear(in_features, NUM_CLASSES)
```
]
\
\

冻结阶段训练新分类头 10 轮。随后解冻第 5 个卷积块 `features[34:]`，微调 5 轮，并为卷积块与分类头设置不同学习率。

#table(
  columns: (1.1fr, 1.4fr, 1.5fr, 0.45fr),
  [阶段], [可训练部分], [优化设置], [轮数],
  [普通 CNN], [整个网络], [Adam；学习率 1e-3], [15],
  [VGG16-BN 冻结], [最后的分类层], [AdamW；学习率 1e-3], [10],
  [VGG16-BN 微调], [第 5 卷积块与最后分类层], [AdamW；卷积块 1e-5，分类层 1e-4], [5],
)

两个 AdamW 阶段均设置权重衰减 $10^(-4)$。



== 实验结果与分析


#table(
  columns: (1.9fr, 1fr),
  [评价项目], [实际结果],
  [普通 CNN 最佳验证准确率（第 11 轮）], [86.06%],
  [VGG16-BN 冻结阶段最佳（第 10 轮）], [94.55%],
  [VGG16-BN 微调阶段自身最高（第 5 轮）], [92.73%],
  [VGG16-BN 最终保留模型的验证准确率], [94.55%],
  [迁移学习相对 CNN 的提升（百分点）], [约 8.48],
  [Kaggle Accuracy Score], [0.9333],
)

#figure(
  image(
    "../assets/24.png",
  ),
  caption: [训练曲线对比图]
)

#figure(
  image(
    "../assets/25.png",
  ),
  caption: [混淆矩阵],
)



非应急车辆有 108 张，precision 为 0.9541、recall 为 0.9630；应急车辆有 57 张，precision 为 0.9286、recall 为 0.9123。整体准确率为 0.9455，macro F1 为 0.9394。应急车辆的召回率相对较低，说明除了总体正确率，还应关注漏识别应急车辆的问题。







#pagebreak()
= 实验二：预训练模型与个性化狗门

== 普通狗门：直接使用 VGG16


05a 直接加载预训练 VGG16，不执行新任务训练。使用权重自带的图像变换后，单张图片变为 1×3×224×224，网络输出 1×1000 的类别分数。使用 `argmax(dim=1)` 沿类别维选择预测索引，再查阅 `imagenet_class_index.json`。
\
\

狗门按索引范围执行规则：151–268 判为狗并放行；281–285 判为猫并留在屋内；其他类别拒绝进入。保存输出如下：



== 网络构建

05b 使用 Penny the Corgi 数据集，按 `Penny` 和 `Not_Penny` 子目录生成标签，编码依次为 0 和 1。Dataset 读取图像并执行 VGG16 权重自带预处理；之后用 `random_split` 按 80%/20% 划分训练集与验证集，batch size 为 32。
\
\
个性化狗门的结构为完整 VGG16 后接一个线性层：

#block(breakable: false)[
```python
vgg_model.requires_grad_(False)
my_model = nn.Sequential(
    vgg_model,
    nn.Linear(1000, 1)
)
loss_function = nn.BCEWithLogitsLoss()
optimizer = Adam(my_model.parameters())
```
]

新增层有 $1000 times 1 + 1 = 1001$ 个参数。冻结检查的输出中，原 VGG16 参数均为 False，新层的权重和偏置为 True。实验先冻结训练 10 轮，采用 Adam 默认学习率 $10^(-3)$；再解冻全部 VGG16 参数，重新创建学习率为 $10^(-6)$ 的 Adam，微调 2 轮。

训练增强包括 ±25° 旋转、面积比例 0.8–1.0 的随机裁剪、水平翻转及颜色扰动。当前实现先执行标准化，再在训练循环对张量增强；这一顺序在颜色处理上存在局限，见后文讨论。

== 冻结阶段的训练结果

准确率由每批预测正确数量累加后除以整个数据集大小得到。以下指标按 notebook 保存的日志记录；为便于阅读，表中轮次从 1 开始，原输出中的 epoch 则从 0 开始。

#table(
  columns: (0.5fr, 1fr, 1fr, 0.9fr, 0.9fr),
  [轮次], [训练准确率], [验证准确率], [训练 Loss], [验证 Loss],
  [1], [68.94%], [84.85%], [3.2595], [0.8876],
  [2], [78.79%], [84.85%], [2.2707], [0.7319],
  [3], [76.52%], [84.85%], [2.2451], [0.7053],
  [4], [74.24%], [87.88%], [2.0107], [0.7721],
  [5], [87.12%], [87.88%], [1.4656], [0.9854],
  [6], [81.06%], [90.91%], [1.7347], [0.9469],
  [7], [81.82%], [84.85%], [1.8280], [0.7475],
  [8], [84.09%], [84.85%], [2.5188], [0.7526],
  [9], [83.33%], [81.82%], [1.6298], [0.7695],
  [10], [89.39%], [81.82%], [1.0817], [0.8001],
)


冻结阶段的最佳验证准确率出现在第 6 轮，为 90.91%；末轮为 81.82%，下降 9.09 个百分点。



== 微调

#table(
  columns: (0.6fr, 1fr, 1fr, 0.9fr, 0.9fr),
  [微调轮次], [训练准确率], [验证准确率], [训练 Loss], [验证 Loss],
  [1], [92.42%], [81.82%], [1.1115], [0.7989],
  [2], [85.61%], [81.82%], [1.7697], [0.7944],
)

微调前末轮验证准确率为 81.82%，微调两轮后仍为 81.82%，提升为 0.00 个百分点。验证 Loss 从 0.8001 略降至 0.7944，但分类正确率没有改善。相对冻结阶段历史最佳 90.91%，最终结果仍低 9.09 个百分点。



== 个性化狗门预测

模型输出一个 logit，使用 `pred < 0` 判为 Penny。

#table(
  columns: (1.5fr, 0.9fr, 1.1fr),
  [图片（类别／文件名）], [logit 或输出], [判定分析],
  [Penny／006.JPG], [-3.7228], [判为 Penny，正确],
  [Not_Penny／021.jpg], [-0.2752], [判为 Penny，误放行],
  [Not_Penny／042.jpg], [这不是 Penny], [拒绝进入，正确],
  [Penny／030.jpg], [是 Penny], [允许进入，正确],
)

其中非 Penny 图片 021.jpg 的输出为负，说明模型可能错误放行其他动物，不能只展示成功案例就断言狗门已经可靠。该值接近阈值 0，也反映出这次预测的分类边界较不明确。
\
\
为测试模型是否放行其他动物，并减少给出图片对预测的干扰，可以使用网络的随机图片进行预测。

#figure(
  image(
    "../assets/26.png",
  ),
  caption: [预测结果成功],
)

= 实验问题与改进思考



== 推理稳定性



Penny 数据处理中，ColorJitter 的色相变换不宜作用于可能含负值的标准化张量。更合理的流程是先在原图或合法像素范围内执行几何和颜色增强，再转为张量并进行 ImageNet 标准化。此处指出的是当前实现的改进方向，不将修正后的效果写作已完成的实验结果。



== 迁移学习效果

迁移学习的价值在于复用已有的视觉表示，使小数据任务能够从较好的参数起点开始学习。冻结、微调、数据增强和模型选择需要配合使用；微调并非必然提高准确率。本次 Penny 日志尤其说明，应根据验证结果选择权重，而不能只按训练轮数或训练准确率判断模型优劣。

\
\
\
\

= 参考资料

+ 课程资料
+ 本报告的部分文字组织、代码核对与排错分析由 OpenAI Codex 辅助完成。所有实验数据、运行输出和技术描述均由本人检查，最终报告由本人审阅并负责。
