#import "template.typ": project, note, warning, analysis

#show: project.with(
  theme: "lab",
  course: "课程综合实践I-\n深度学习基础——理论与实践",
  title: "实验七实验报告",
  name: "实验七：生成对抗网络GAN",
  author: "李昊泽",
  school_id: "3250102780",
  college: "计算机科学与技术学院",
  major: "计算机科学与技术",
  teacher: "王总辉",
  place: "紫金港海洋大楼11机房",
  date: "2026年9月8日",
  font_serif: ("Songti SC", "New Computer Modern"),
  font_sans_serif: ("PingFang SC", "Helvetica Neue"),
  font_mono: ("Monaco",),
)

// 补图方法：将对应 screenshot(...) 整段替换为
// #figure(image("images/lab7-01.png", width: 100%), caption: [对应图题])
// height 可按截图比例调整。本报告只保留截图区域，不依赖图片文件。
#let screenshot(title, suggestion, height: 5.5cm) = figure(
  block(
    width: 100%,
    height: height,
    fill: white,
    stroke: (paint: luma(165), thickness: 0.8pt, dash: "dashed"),
    radius: 4pt,
    inset: 14pt,
    align(center + horizon)[
      #text(size: 12pt, weight: "bold", fill: luma(85))[截图留空区]
      #v(0.8em)
      #text(size: 10pt, fill: luma(115))[#suggestion]
    ],
  ),
  kind: image,
  caption: title,
)

#set table(inset: 6pt)

= 摘要

本实验实现了无条件生成对抗网络和条件生成对抗网络。实验一使用 71,314 张动漫人脸训练 DCGAN，将 100 维随机噪声逐步上采样为 64×64 RGB 图像；完整训练 1 个 epoch 后，判别器和生成器平均 BCE 损失分别为 0.2529 和 6.1487，并成功生成、保存 100 张动漫人脸。
\
\
实验二在 60,000 张 MNIST 训练图像上训练条件 GAN，把数字类别作为附加条件输入生成器和判别器；完整训练 10 个 epoch 后，判别器损失为 0.5365，生成器损失为 1.1833，模型能够按标签生成 0-9，并能单独生成数字 7。
\
\









= 实验原理

== 生成对抗网络

GAN 由生成器 $G$ 和判别器 $D$ 组成。生成器接收随机噪声 $z$，输出生成样本 $G(z)$；判别器接收真实样本或生成样本，输出其为真实数据的概率。经典 GAN 的目标可写为：

$ min_G max_D V(D, G) = "E"_x [ln D(x)] + "E"_z [ln(1 - D(G(z)))] $

判别器希望增大真实样本的输出并减小生成样本的输出；生成器则希望使 $D(G(z))$ 接近 1。两个网络交替更新，因此训练不是普通的单目标最小化，而是动态博弈过程。


== DCGAN 与转置卷积

实验一采用 DCGAN。生成器先用全连接层把低维噪声映射为小尺寸多通道特征图，再用转置卷积逐步扩大空间尺寸。若普通卷积用于下采样和提取判别特征，转置卷积则通过可学习权重完成上采样。生成器中使用 BatchNorm 和 ReLU 稳定训练，最后使用 Tanh，使输出范围与归一化后的真实图像 $[-1,1]$ 一致。

判别器使用步长为 2 的卷积逐步缩小图像，配合 LeakyReLU 保留负半轴梯度，最后通过 Sigmoid 得到真假概率。实验中的权重以均值 0、标准差 0.02 的正态分布初始化。

== 条件 GAN

普通 GAN 只学习 $G(z)$，无法直接控制输出类别。条件 GAN 引入标签 $y$，把两个网络写为 $G(z,y)$ 和 $D(x,y)$。实验二把数字标签通过嵌入层转换为连续向量：生成器将 100 维噪声与 10 维标签表示拼接，判别器将展平后的图像与 784 维标签表示拼接。

条件信息同时约束生成和判别：生成器不仅要生成“像手写数字”的图像，还要与指定标签一致；判别器则判断“图像和标签这一组合”是否来自真实数据。因此，在噪声不同而标签相同的情况下，模型应生成书写风格不同但类别一致的数字。

== GAN 损失与生成质量

GAN 的损失不能按普通分类任务的方式单调解释。判别器过强时，$L_D$ 可能很低而 $L_G$ 很高；生成器改善后，判别器面对更逼真的假样本，$L_D$ 反而可能上升。因此本实验同时保存固定噪声的阶段性生成结果，通过相同输入在不同 epoch 的变化判断模型是否学到轮廓、类别与细节。


#pagebreak()

= 实验一：DCGAN 动漫人脸生成

== 数据预处理



+ 将图像统一转换为 RGB。
+ 缩放到 64×64。
+ 转换为通道优先的张量 $3 times 64 times 64$。
+ 按三个通道的均值 0.5、标准差 0.5 归一化，使像素范围由 $[0,1]$ 映射到 $[-1,1]$。



== 网络结构

生成器输入为 100 维标准正态噪声，先映射为 $512 times 4 times 4$ 特征图，再通过四层转置卷积生成 RGB 图像：

#table(
  columns: (1.55fr, 1.25fr, 1.75fr),
  [*生成阶段*], [*输出尺寸*], [*激活与归一化*],
  [Linear、reshape], [$512 times 4 times 4$], [BatchNorm、ReLU],
  [转置卷积 512 → 256], [$256 times 8 times 8$], [BatchNorm、ReLU],
  [转置卷积 256 → 128], [$128 times 16 times 16$], [BatchNorm、ReLU],
  [转置卷积 128 → 64], [$64 times 32 times 32$], [BatchNorm、ReLU],
  [转置卷积 64 → 3], [$3 times 64 times 64$], [Tanh],
)



== 训练设置

#table(
  columns: (1.2fr, 1fr, 1.2fr, 1fr),
  [*参数*], [*取值*], [*参数*], [*取值*],
  [随机种子], [2021], [噪声维数], [100],
  [batch size], [64], [训练轮数], [1],
  [每轮批次数], [1114], [学习率], [$10^(-4)$],
  [损失函数], [BCELoss], [优化器], [Adam],
  [Adam betas], [`(0.5, 0.999)`], [计算设备], [CPU],
)

每个批次先更新判别器：分别计算真实图像与生成图像的 BCE，并取二者平均；随后更新生成器，使同一批生成图像被判别器判断为真实。生成图像在判别器更新时使用 `detach`，避免判别器反向传播同时修改生成器。

== 实验结果与分析

完整 1 个 epoch 的实际结果如下：

#table(
  columns: (1.8fr, 1.2fr),
  [*评价项目*], [*实际结果*],
  [判别器平均 BCE], [0.2529],
  [生成器平均 BCE], [6.1487],
)

#figure(
  image(
    "../assets/31.png",
  ),
  caption: [生成结果],
)



生成结果已出现动漫人脸的整体结构，能够观察到头发、眼睛、肤色和脸部轮廓，并且颜色与发型存在一定差异，说明模型在一个 epoch 内已学习到主要视觉分布。但图像仍有较明显的颗粒噪声，部分眼睛、嘴部和轮廓模糊；不同样本的构图也较相似。
\
\



#pagebreak()

= 实验二：条件 GAN 手写数字生成


== 条件生成器

数字标签首先经过 `Embedding(10, 10)` 得到 10 维向量，再与 100 维噪声拼接为 110 维输入。网络结构为：

$ 110 arrow 256 arrow 512 arrow 1024 arrow 784 $

前三个全连接层后使用 ReLU，最后使用 Tanh，并将 784 维输出还原为 $1 times 28 times 28$。生成器共有 1,489,012 个可训练参数。

== 条件判别器

真实或生成图像展平为 784 维，标签通过 `Embedding(10, 784)` 得到同尺寸条件表示，二者拼接为 1568 维。网络结构为：

$ 1568 arrow 1024 arrow 512 arrow 256 arrow 1 $

隐藏层使用斜率 0.2 的 LeakyReLU，最后使用 Sigmoid。判别器共有 2,270,881 个可训练参数。形状测试显示，4 张生成图像的输出为 `(4, 1, 28, 28)`，对应判别结果为 `(4, 1)`。



== 训练设置

#table(
  columns: (1.2fr, 1fr, 1.2fr, 1fr),
  [*参数*], [*取值*], [*参数*], [*取值*],
  [随机种子], [42], [噪声维数], [100],
  [类别数], [10], [batch size], [64],
  [训练轮数], [10], [每轮批次数], [938],
  [学习率], [$2 times 10^(-4)$], [损失函数], [BCELoss],
  [优化器], [Adam], [Adam betas], [`(0.5, 0.999)`],
)

#figure(
  image(
    "../assets/32.png",
  ),
  caption: [训练曲线],
)

== 损失变化

#table(
  columns: (0.55fr, 1fr, 1fr, 0.55fr, 1fr, 1fr),
  [*轮次*], [*$L_D$*], [*$L_G$*], [*轮次*], [*$L_D$*], [*$L_G$*],
  [1], [0.3423], [1.9957], [6], [0.4464], [1.5198],
  [2], [0.3021], [2.1338], [7], [0.4777], [1.3927],
  [3], [0.3375], [1.9634], [8], [0.5033], [1.2950],
  [4], [0.3964], [1.6923], [9], [0.5202], [1.2392],
  [5], [0.4154], [1.6230], [10], [0.5365], [1.1833],
)

第 2 轮生成器损失短暂升至 2.1338，之后总体下降到 1.1833；判别器损失从早期约 0.30 逐步上升到 0.5365。这说明生成器逐渐提高了欺骗判别器的能力，使两者的对抗关系更接近平衡。



== 生成结果

第 1 个 epoch 时，数字轮廓中仍有较多噪点，部分数字难以辨认；到第 5 个 epoch，0-9 的主要结构已形成；第 10 个 epoch 时，同一行的 5 张图片基本都符合该行标签，同时笔画粗细和倾斜程度存在差异。该变化表明条件信息控制有效，随机噪声则保留了同类样本内部的多样性。

#figure(
  image(
    "../assets/33.png",
  ),

)


将条件标签固定为 7 后，模型生成的 16 张图像均具有数字 7 的主要笔画结构，说明模型能够按指定类别生成，而不是只能随机产生任意数字。样本之间在线条弯曲、倾斜和粗细上存在差异；少量样本仍有断裂或局部模糊，说明训练轮数和全连接结构仍限制了细节质量。




= 讨论与改进



+ 动漫人脸只训练 1 个 epoch，虽然已生成可辨识轮廓，但细节仍较粗糙；需要增加训练轮数并保存更多中间样例。
+ 两个实验主要采用人工观察生成质量，尚未计算 FID、KID 等分布级指标，也没有独立评估样本多样性。
+ MNIST 条件 GAN 使用全连接网络，忽略了图像的二维局部结构；可改为条件卷积 GAN，提高笔画连续性。
+ 当前运行使用 CPU，完整训练耗时较长；若迁移到 GPU，应重新核对设备、随机性与实际运行日志，不能直接把 CPU 结果视为 GPU 复现结果。




= 参考资料

+ 课程资料
+ 本报告的部分文字组织、代码核对与排错分析由 OpenAI Codex 辅助完成。所有实验数据、运行输出和技术描述均由本人检查，最终报告由本人审阅并负责。
